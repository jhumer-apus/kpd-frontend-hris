import { DataGrid } from "@mui/x-data-grid";
import { Fragment } from "react";

export default function FamilyBakcgroundTable() {
    return (
        <Fragment>
            <DataGrid 
                columns={[]} 
                rows={[]} 
            />
        </Fragment>
    )
}